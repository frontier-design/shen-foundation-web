# Pages CMS config — https://pagescms.org/docs/configuration
media:
  input: public/media
  output: /media
  categories: [image]
  rename: safe

content:
  - name: home
    label: Home
    type: file
    path: content/pages/home.json
    format: json
    operations:
      delete: false
      rename: false
    fields:
      - name: heroSlides
        label: Hero carousel
        type: reference
        options:
          collection: exhibitions
          multiple: true
          search: title,subtitle
          label: "{title} — {subtitle}"
      - name: callout
        label: Homepage callout
        type: object
        fields:
          - name: enabled
            label: Show callout
            type: boolean
          - name: feature
            label: Featured item
            type: block
            blockKey: type
            blocks:
              - name: exhibition
                label: Exhibition
                fields:
                  - name: exhibition
                    label: Exhibition
                    type: reference
                    required: true
                    options:
                      collection: exhibitions
                      search: title,subtitle
                      label: "{title} — {subtitle}"
              - name: event
                label: Event
                fields:
                  - name: event
                    label: Event
                    type: reference
                    required: true
                    options:
                      collection: events
                      search: title
                      label: "{title}"
          - name: image
            label: Image (portrait)
            type: image
            required: true
            description: "Portrait only. Landscape images are not accepted here."
            options:
              categories: [image]
              path: public/media
      - name: grid
        label: Homepage grid
        type: block
        blockKey: type
        list:
          collapsible:
            collapsed: true
            summary: "{exhibition}{event}"
        blocks:
          - name: exhibition
            label: Exhibition
            fields:
              - name: exhibition
                label: Exhibition
                type: reference
                required: true
                options:
                  collection: exhibitions
                  search: title,subtitle
                  value: subtitle
                  label: "{title} — {subtitle}"
          - name: event
            label: Event
            fields:
              - name: event
                label: Event
                type: reference
                required: true
                options:
                  collection: events
                  search: title
                  value: title
                  label: "{title}"

  - name: artistsPage
    label: Artists page
    type: file
    path: content/pages/artists.json
    format: json
    operations:
      delete: false
      rename: false
    fields:
      - name: artistOrder
        label: Artist order
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{artist}"
        fields:
          - name: artist
            label: Artist
            type: reference
            options:
              collection: artists
              search: title
              value: title
              label: "{title}"

  - name: aboutPage
    label: About page
    type: file
    path: content/pages/about.json
    format: json
    operations:
      delete: false
      rename: false
    fields:
      - name: heroImage
        label: Hero image (full bleed)
        type: image
        description: "Fills the top of the page behind the white card."
        options:
          categories: [image]
          path: public/media
      - name: heroBody
        label: About Us body copy
        type: text
        description: "Shown under the 'About Us' title in the white hero card."
        options:
          maxlength: 600
      - name: intro
        label: Intro section
        type: object
        description: "Title and body blocks directly under the hero. Drag to reorder."
        list:
          collapsible:
            collapsed: true
            summary: "{title}"
        fields:
          - name: title
            label: Title
            type: string
            options:
              maxlength: 120
          - name: body
            label: Body
            type: text
            description: "Shown beside the title."
            options:
              maxlength: 1200
      - name: people
        label: People
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{name} — {role}"
        fields:
          - name: name
            label: Name
            type: string
            options:
              maxlength: 120
          - name: role
            label: Role
            type: string
            options:
              maxlength: 120
          - name: bio
            label: Bio
            type: text
            options:
              maxlength: 2000
          - name: photo
            label: Photo
            type: image
            options:
              categories: [image]
              path: public/media

  - name: artists
    label: Artists
    type: collection
    path: content/artists
    format: json
    filename:
      template: "{fields.title}.json"
      field: false
    view:
      fields: [title]
      primary: title
      sort: [title]
      search: [title]
      default:
        sort: title
        order: asc
    fields:
      - name: title
        label: Title
        type: string
        required: true
        options:
          maxlength: 120
      - name: bio
        label: Bio
        type: text
        options:
          maxlength: 2000
      - name: thumbnail
        label: Thumbnail
        type: image
        options:
          categories: [image]
          path: public/media

  - name: exhibitions
    label: Exhibitions
    type: collection
    path: content/exhibitions
    format: json
    filename:
      template: "{fields.subtitle}.json"
      field: false
    view:
      fields: [title, subtitle, status]
      primary: title
      sort: [title]
      search: [title, subtitle]
      default:
        sort: title
        order: asc
    fields:
      - name: title
        label: Title
        type: string
        required: true
        options:
          maxlength: 120
      - name: status
        label: Status
        type: select
        default: ongoing
        description: "Controls which tab this appears under on the Exhibitions & Projects page."
        options:
          values: [ongoing, archive]
      - name: subtitle
        label: Artwork
        type: string
        required: true
        description: "The project/exhibition name. Also used to generate the page URL."
        options:
          maxlength: 160
      - name: heroImage
        label: Hero image (landscape)
        type: image
        description: "Landscape only. Also used as the project thumbnail in grids. Portrait images are not accepted."
        options:
          categories: [image]
          path: public/media
      - name: startDate
        label: Start date
        type: date
        default: ""
        options:
          format: yyyy-MM-dd
      - name: endDate
        label: End date (optional)
        type: date
        default: ""
        description: "Leave empty for a single-day date."
        options:
          format: yyyy-MM-dd
      - name: captionLocation
        label: Location
        type: string
      - name: body
        label: Body text
        type: text
        options:
          maxlength: 1200
      - name: gallery
        label: Gallery
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{caption}"
        fields:
          - name: image
            label: Image
            type: image
            description: "Any dimensions."
            options:
              categories: [image]
              path: public/media
          - name: caption
            label: Caption
            type: string

  - name: events
    label: Events
    type: collection
    path: content/events
    format: json
    filename:
      template: "{fields.title}.json"
      field: false
    view:
      fields: [title, captionLabel, status]
      primary: title
      sort: [title]
      search: [title]
      default:
        sort: title
        order: asc
    fields:
      - name: title
        label: Title
        type: string
        required: true
        options:
          maxlength: 120
      - name: status
        label: Status
        type: select
        default: ongoing
        description: "Archived events move to the Archive tab on the Events page."
        options:
          values: [ongoing, archive]
      - name: captionLabel
        label: Event Type
        type: string
        description: "e.g. Talk, Workshop, Tour. Becomes a filter tab on the Events page."
      - name: startDate
        label: Start date
        type: date
        default: ""
        options:
          format: yyyy-MM-dd
      - name: endDate
        label: End date (optional)
        type: date
        default: ""
        description: "Leave empty for a single-day date."
        options:
          format: yyyy-MM-dd
      - name: captionLocation
        label: Location
        type: string
      - name: image
        label: Thumbnail image (landscape)
        type: image
        description: "Landscape only. Portrait images are not accepted."
        options:
          categories: [image]
          path: public/media
      - name: sections
        label: Sections
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{title}"
        fields:
          - name: title
            label: Title
            type: string
          - name: content
            label: Content
            type: text

settings:
  content:
    merge: true
